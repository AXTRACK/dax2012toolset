Exportfile for AOT version 1.0 or later
Formatversion: 1

***Element: FRM

; Microsoft Dynamics AX Forms unloaded
; --------------------------------------------------------------------------------
FRMVERSION 12

FORM #DEV_WebSafeColors
  PROPERTIES
    Name                #DEV_WebSafeColors
    Origin              #{CFF2AACF-C8AD-4BC9-9DD6-BEDDC432E061}
  ENDPROPERTIES
  
  METHODS
    SOURCE #classDeclaration
      #// DEV:MXK
      #/// <summary>
      #/// This form provides a set of colors.
      #/// Could be used to quickly define appropriate color values Web or RGB.
      #/// </summary>
      #class FormRun extends ObjectRun
      #{
      #    Set                             cSet;
      #
      #    #DEV_Toolset
      #
      #    #localmacro.copyColor
      #        FormStringControl                           fsc         = element.controlCallingMethod();
      #        container                                   c           = str2con(fsc.text(), '\n');
      #        TextBuffer                                  textBuffer  = new TextBuffer();
      #
      #        textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                                    subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                                    subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                                    subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #        textBuffer.toClipboard();
      #    #endmacro
      #}
    ENDSOURCE
    SOURCE #cell111_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell111_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #
      #    Box::infoOnceModal( #ToolsetName,
      #                        'Color pick',
      #                        'Selected color has been copied to the clipboard.',
      #                        curUserId());
      #}
    ENDSOURCE
    SOURCE #cell112_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell112_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell113_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell113_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell114_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell114_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell115_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell115_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell116_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell116_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell121_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell121_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell122_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell122_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell123_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell123_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell124_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell124_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell125_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell125_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell126_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell126_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell131_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell131_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell132_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell132_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell133_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell133_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell134_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell134_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell135_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell135_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell136_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell136_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell141_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell141_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell142_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell142_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell143_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell143_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell144_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell144_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell145_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell145_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell146_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell146_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell151_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell151_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell152_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell152_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell153_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell153_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell154_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell154_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell155_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell155_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell156_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell156_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell161_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell161_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell162_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell162_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell163_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell163_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell164_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell164_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell165_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell165_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell166_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell166_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell211_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell211_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell212_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell212_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell213_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell213_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell214_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell214_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell215_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell215_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell216_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell216_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell221_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell221_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell222_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell222_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell223_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell223_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell224_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell224_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell225_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell225_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell226_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell226_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell231_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell231_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell232_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell232_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell233_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell233_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell234_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell234_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell235_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell235_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell236_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell236_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell241_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell241_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell242_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell242_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell243_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell243_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell244_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell244_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell245_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell245_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell246_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell246_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell251_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell251_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell252_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell252_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell253_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell253_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell254_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell254_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell255_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell255_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell256_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell256_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell261_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell261_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell262_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell262_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell263_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell263_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell264_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell264_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell265_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell265_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell266_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell266_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell311_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell311_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell312_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell312_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell313_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell313_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell314_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell314_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell315_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell315_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell316_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell316_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell321_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell321_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell322_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell322_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell323_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell323_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell324_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell324_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell325_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell325_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell326_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell326_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell331_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell331_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell332_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell332_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell333_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell333_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell334_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell334_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell335_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell335_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell336_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell336_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell341_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell341_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell342_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell342_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell343_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell343_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell344_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell344_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell345_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell345_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell346_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell346_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell351_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell351_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell352_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell352_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell353_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell353_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell354_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell354_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell355_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell355_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell356_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell356_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell361_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell361_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell362_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell362_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell363_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell363_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell364_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell364_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell365_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell365_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell366_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell366_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell411_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell411_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell412_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell412_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell413_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell413_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell414_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell414_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell415_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell415_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell416_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell416_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell421_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell421_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell422_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell422_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell423_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell423_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell424_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell424_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell425_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell425_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell426_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell426_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell431_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell431_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell432_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell432_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell433_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell433_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell434_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell434_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell435_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell435_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell436_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell436_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell441_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell441_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell442_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell442_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell443_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell443_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell444_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell444_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell445_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell445_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell446_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell446_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell451_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell451_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell452_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell452_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell453_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell453_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell454_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell454_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell455_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell455_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell456_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell456_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell461_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell461_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell462_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell462_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell463_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell463_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell464_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell464_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell465_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell465_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell466_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell466_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell511_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell511_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell512_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell512_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell513_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell513_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell514_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell514_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell515_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell515_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell516_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell516_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell521_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell521_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell522_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell522_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell523_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell523_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell524_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell524_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell525_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell525_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell526_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell526_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell531_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell531_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell532_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell532_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell533_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell533_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell534_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell534_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell535_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell535_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell536_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell536_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell541_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell541_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell542_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell542_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell543_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell543_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell544_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell544_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell545_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell545_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell546_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell546_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell551_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell551_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell552_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell552_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell553_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell553_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell554_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell554_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell555_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell555_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell556_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell556_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell561_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell561_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell562_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell562_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell563_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell563_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell564_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell564_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell565_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell565_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell566_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell566_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell611_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell611_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell612_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell612_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell613_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell613_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell614_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell614_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell615_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell615_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell616_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell616_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell621_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell621_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell622_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell622_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell623_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell623_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell624_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell624_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell625_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell625_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell626_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell626_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell631_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell631_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell632_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell632_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell633_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell633_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell634_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell634_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell635_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell635_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell636_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell636_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell641_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell641_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell642_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell642_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell643_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell643_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell644_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell644_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell645_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell645_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell646_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell646_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell651_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell651_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell652_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell652_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell653_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell653_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell654_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell654_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell655_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell655_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell656_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell656_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell661_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell661_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell662_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell662_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell663_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell663_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell664_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell664_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell665_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell665_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #cell666_gotFocus
      #// Method generated by job DEV_WebSafeColors_Update
      #public void cell666_gotFocus()
      #{
      #    FormStringControl   fsc         = element.controlCallingMethod();
      #    container           c           = str2con(fsc.text(), '\n');
      #    TextBuffer          textBuffer  = new TextBuffer();
      #
      #    textBuffer.appendText(strFmt('WinAPI::RGB2int(%1, %2, %3)',
      #                            subStr(conPeek(c, 2), 3, strLen(conPeek(c, 2)) - 2),
      #                            subStr(conPeek(c, 3), 3, strLen(conPeek(c, 3)) - 2),
      #                            subStr(conPeek(c, 4), 3, strLen(conPeek(c, 4)) - 2)));
      #
      #    textBuffer.toClipboard();
      #}
    ENDSOURCE
    SOURCE #getColor
      #str getColor(   int     _iteration,
      #                int     _row,
      #                int     _col)
      #{
      #    Map                         map = new Map(Types::Integer, Types::String);
      #    str                         ret;
      #
      #    map.insert(1, 'FF');
      #    map.insert(2, 'CC');
      #    map.insert(3, '99');
      #    map.insert(4, '66');
      #    map.insert(5, '33');
      #    map.insert(6, '00');
      #
      #    if (_iteration)
      #        ret += map.lookup(_iteration);
      #
      #    if (_row)
      #        ret += map.lookup(_row);
      #
      #    if (_col)
      #        ret += map.lookup(_col);
      #
      #    return ret;
      #}
    ENDSOURCE
    SOURCE #run
      #public void run()
      #{
      #    element.controlMethodOverload(true);
      #
      #    MainTitleStaticText.setFocus();
      #
      #    super();
      #
      #    DEV_Global::showHint('This is a simple form that allows quickly copy required RGB color code.\n' +
      #                        'Just click on the color required and code will be copied into clipboard.',
      #                        this.name(),
      #                        'Pick color');
      #}
    ENDSOURCE
  ENDMETHODS
  OBJECTBANK
    PROPERTIES
    ENDPROPERTIES
    
  ENDOBJECTBANK
  
  REFERENCEDATASOURCES
  ENDREFERENCEDATASOURCES
  
  JOINS
  ENDJOINS
  
  PARTREFERENCES
  ENDPARTREFERENCES
  
  DESIGN
    PROPERTIES
      Width               #Column width
      Height              #Column height
      Caption             #216 Web Safe Color Chart
      HideToolbar         #Yes
    ENDPROPERTIES
    
    CONTAINER
      CONTROL GROUP
        PROPERTIES
          Name                #MainTitle
          ElementPosition     #536870911
          Style               #TOCTitleContainer
        ENDPROPERTIES
        
        METHODS
        ENDMETHODS
      ENDCONTROL 
      
      CONTROL TAB
        PROPERTIES
          Name                #Tab
          ElementPosition     #1073741823
        ENDPROPERTIES
        
        METHODS
        ENDMETHODS
      ENDCONTROL 
      
      CONTROL TABLE
        PROPERTIES
          Name                #Table1
          AutoDeclaration     #Yes
          AllowEdit           #No
          VerticalSpacing     #0
          Skip                #Yes
          ElementPosition     #1431655764
          HierarchyParent     #TabPage1
          BackgroundColor     #Window background
          Columns             #6
          Rows                #6
          ShowColLabels       #No
          ShowRowLabels       #No
        ENDPROPERTIES
        
        METHODS
          SOURCE #editControl
            #FormControl editControl(int column, int row)
            #{
            #    int                                 it = 1;
            #    FormStringControl                   fsc;
            #
            #    if (!cSet)
            #        cSet = new Set(Types::String);
            #
            #    if (cSet.in(strFmt('Cell%1%2%3', it, row, column)))
            #    {
            #        fsc = table1.controlNum((column - 1)*6 + row + 1);
            #    }
            #    else
            #    {
            #        fsc = table1.addControl(FormControlType::String, strFmt('Cell%1%2%3', it, row, column));
            #        fsc.alignment(3);
            #        fsc.colorScheme(FormColorScheme::RGB);
            #        fsc.limitText(50);
            #        fsc.multiLine(true);
            #        fsc.width(100);
            #        fsc.backgroundColor(WinAPI::rgb2int(hex2Int(element.getColor(it, 0, 0)),
            #                                            hex2Int(element.getColor(0, row, 0)),
            #                                            hex2Int(element.getColor(0, 0, column))));
            #
            #        if (row <= 3)
            #        {
            #            fsc.foregroundColor(WinAPI::rgb2int(0, 0, 0));
            #        }
            #        else
            #        {
            #            fsc.foregroundColor(WinAPI::rgb2int(255, 255, 255));
            #        }
            #
            #        // BP Deviation Documented
            #        table1.cell(column, row).data(strFmt('# %1\nR=%2\nG=%3\nB=%4',
            #                                    element.getColor(it, row, column),
            #                                    hex2Int(element.getColor(it, 0, 0)),
            #                                    hex2Int(element.getColor(0, row, 0)),
            #                                    hex2Int(element.getColor(0, 0, column))));
            #
            #        cSet.add(fsc.name());
            #    }
            #
            #    return fsc;
            #}
          ENDSOURCE
        ENDMETHODS
      ENDCONTROL 
      
      CONTROL TABLE
        PROPERTIES
          Name                #Table2
          AutoDeclaration     #Yes
          AllowEdit           #No
          VerticalSpacing     #0
          ElementPosition     #1431655764
          HierarchyParent     #TabPage2
          BackgroundColor     #Window background
          Columns             #6
          Rows                #6
          ShowColLabels       #No
          ShowRowLabels       #No
        ENDPROPERTIES
        
        METHODS
          SOURCE #editControl
            #FormControl editControl(int column, int row)
            #{
            #    int                                 it = 2;
            #    FormStringControl                   fsc;
            #
            #    if (!cSet)
            #        cSet = new Set(Types::String);
            #
            #    if (cSet.in(strFmt('Cell%1%2%3', it, row, column)))
            #    {
            #        fsc = table2.controlNum((column - 1)*6 + row + 1);
            #    }
            #    else
            #    {
            #        fsc = table2.addControl(FormControlType::String, strFmt('Cell%1%2%3', it, row, column));
            #        fsc.alignment(3);
            #        fsc.colorScheme(FormColorScheme::RGB);
            #        fsc.limitText(50);
            #        fsc.multiLine(true);
            #        fsc.width(100);
            #        fsc.backgroundColor(WinAPI::rgb2int(hex2Int(element.getColor(it, 0, 0)),
            #                                            hex2Int(element.getColor(0, row, 0)),
            #                                            hex2Int(element.getColor(0, 0, column))));
            #
            #        if (row <= 3)
            #        {
            #            fsc.foregroundColor(WinAPI::rgb2int(0, 0, 0));
            #        }
            #        else
            #        {
            #            fsc.foregroundColor(WinAPI::rgb2int(255, 255, 255));
            #        }
            #
            #        // BP Deviation Documented
            #        table2.cell(column, row).data(strFmt('# %1\nR=%2\nG=%3\nB=%4',
            #                                    element.getColor(it, row, column),
            #                                    hex2Int(element.getColor(it, 0, 0)),
            #                                    hex2Int(element.getColor(0, row, 0)),
            #                                    hex2Int(element.getColor(0, 0, column))));
            #
            #        cSet.add(fsc.name());
            #    }
            #
            #    return fsc;
            #}
          ENDSOURCE
        ENDMETHODS
      ENDCONTROL 
      
      CONTROL TABLE
        PROPERTIES
          Name                #Table3
          AutoDeclaration     #Yes
          AllowEdit           #No
          VerticalSpacing     #0
          ElementPosition     #1431655764
          HierarchyParent     #TabPage3
          BackgroundColor     #Window background
          Columns             #6
          Rows                #6
          ShowColLabels       #No
          ShowRowLabels       #No
        ENDPROPERTIES
        
        METHODS
          SOURCE #editControl
            #FormControl editControl(int column, int row)
            #{
            #    int                                 it = 3;
            #    FormStringControl                   fsc;
            #
            #    if (!cSet)
            #        cSet = new Set(Types::String);
            #
            #    if (cSet.in(strFmt('Cell%1%2%3', it, row, column)))
            #    {
            #        fsc = table3.controlNum((column - 1)*6 + row + 1);
            #    }
            #    else
            #    {
            #        fsc = table3.addControl(FormControlType::String, strFmt('Cell%1%2%3', it, row, column));
            #        fsc.alignment(3);
            #        fsc.colorScheme(FormColorScheme::RGB);
            #        fsc.limitText(50);
            #        fsc.multiLine(true);
            #        fsc.width(100);
            #        fsc.backgroundColor(WinAPI::rgb2int(hex2Int(element.getColor(it, 0, 0)),
            #                                            hex2Int(element.getColor(0, row, 0)),
            #                                            hex2Int(element.getColor(0, 0, column))));
            #
            #        if (row <= 3)
            #        {
            #            fsc.foregroundColor(WinAPI::rgb2int(0, 0, 0));
            #        }
            #        else
            #        {
            #            fsc.foregroundColor(WinAPI::rgb2int(255, 255, 255));
            #        }
            #
            #        // BP Deviation Documented
            #        table3.cell(column, row).data(strFmt('# %1\nR=%2\nG=%3\nB=%4',
            #                                    element.getColor(it, row, column),
            #                                    hex2Int(element.getColor(it, 0, 0)),
            #                                    hex2Int(element.getColor(0, row, 0)),
            #                                    hex2Int(element.getColor(0, 0, column))));
            #
            #        cSet.add(fsc.name());
            #    }
            #
            #    return fsc;
            #}
          ENDSOURCE
        ENDMETHODS
      ENDCONTROL 
      
      CONTROL TABLE
        PROPERTIES
          Name                #Table4
          AutoDeclaration     #Yes
          AllowEdit           #No
          VerticalSpacing     #0
          ElementPosition     #1431655764
          HierarchyParent     #TabPage4
          BackgroundColor     #Window background
          Columns             #6
          Rows                #6
          ShowColLabels       #No
          ShowRowLabels       #No
        ENDPROPERTIES
        
        METHODS
          SOURCE #editControl
            #FormControl editControl(int column, int row)
            #{
            #    int                                 it = 4;
            #    FormStringControl                   fsc;
            #
            #    if (!cSet)
            #        cSet = new Set(Types::String);
            #
            #    if (cSet.in(strFmt('Cell%1%2%3', it, row, column)))
            #    {
            #        fsc = table4.controlNum((column - 1)*6 + row + 1);
            #    }
            #    else
            #    {
            #        fsc = table4.addControl(FormControlType::String, strFmt('Cell%1%2%3', it, row, column));
            #        fsc.alignment(3);
            #        fsc.colorScheme(FormColorScheme::RGB);
            #        fsc.limitText(50);
            #        fsc.multiLine(true);
            #        fsc.width(100);
            #        fsc.backgroundColor(WinAPI::rgb2int(hex2Int(element.getColor(it, 0, 0)),
            #                                            hex2Int(element.getColor(0, row, 0)),
            #                                            hex2Int(element.getColor(0, 0, column))));
            #
            #        if (row <= 3)
            #        {
            #            fsc.foregroundColor(WinAPI::rgb2int(0, 0, 0));
            #        }
            #        else
            #        {
            #            fsc.foregroundColor(WinAPI::rgb2int(255, 255, 255));
            #        }
            #
            #        // BP Deviation Documented
            #        table4.cell(column, row).data(strFmt('# %1\nR=%2\nG=%3\nB=%4',
            #                                    element.getColor(it, row, column),
            #                                    hex2Int(element.getColor(it, 0, 0)),
            #                                    hex2Int(element.getColor(0, row, 0)),
            #                                    hex2Int(element.getColor(0, 0, column))));
            #
            #        cSet.add(fsc.name());
            #    }
            #
            #    return fsc;
            #}
          ENDSOURCE
        ENDMETHODS
      ENDCONTROL 
      
      CONTROL TABLE
        PROPERTIES
          Name                #Table5
          AutoDeclaration     #Yes
          AllowEdit           #No
          VerticalSpacing     #0
          ElementPosition     #1431655764
          HierarchyParent     #TabPage5
          BackgroundColor     #Window background
          Columns             #6
          Rows                #6
          ShowColLabels       #No
          ShowRowLabels       #No
        ENDPROPERTIES
        
        METHODS
          SOURCE #editControl
            #FormControl editControl(int column, int row)
            #{
            #    int                                 it = 5;
            #    FormStringControl                   fsc;
            #
            #    if (!cSet)
            #        cSet = new Set(Types::String);
            #
            #    if (cSet.in(strFmt('Cell%1%2%3', it, row, column)))
            #    {
            #        fsc = table5.controlNum((column - 1)*6 + row + 1);
            #    }
            #    else
            #    {
            #        fsc = table5.addControl(FormControlType::String, strFmt('Cell%1%2%3', it, row, column));
            #        fsc.alignment(3);
            #        fsc.colorScheme(FormColorScheme::RGB);
            #        fsc.limitText(50);
            #        fsc.multiLine(true);
            #        fsc.width(100);
            #        fsc.backgroundColor(WinAPI::rgb2int(hex2Int(element.getColor(it, 0, 0)),
            #                                            hex2Int(element.getColor(0, row, 0)),
            #                                            hex2Int(element.getColor(0, 0, column))));
            #
            #        if (row <= 3)
            #        {
            #            fsc.foregroundColor(WinAPI::rgb2int(0, 0, 0));
            #        }
            #        else
            #        {
            #            fsc.foregroundColor(WinAPI::rgb2int(255, 255, 255));
            #        }
            #
            #        // BP Deviation Documented
            #        table5.cell(column, row).data(strFmt('# %1\nR=%2\nG=%3\nB=%4',
            #                                    element.getColor(it, row, column),
            #                                    hex2Int(element.getColor(it, 0, 0)),
            #                                    hex2Int(element.getColor(0, row, 0)),
            #                                    hex2Int(element.getColor(0, 0, column))));
            #
            #        cSet.add(fsc.name());
            #    }
            #
            #    return fsc;
            #}
          ENDSOURCE
        ENDMETHODS
      ENDCONTROL 
      
      CONTROL TABLE
        PROPERTIES
          Name                #Table6
          AutoDeclaration     #Yes
          AllowEdit           #No
          VerticalSpacing     #0
          ElementPosition     #1431655764
          HierarchyParent     #TabPage6
          BackgroundColor     #Window background
          Columns             #6
          Rows                #6
          ShowColLabels       #No
          ShowRowLabels       #No
        ENDPROPERTIES
        
        METHODS
          SOURCE #editControl
            #FormControl editControl(int column, int row)
            #{
            #    int                                 it = 6;
            #    FormStringControl                   fsc;
            #
            #    if (!cSet)
            #        cSet = new Set(Types::String);
            #
            #    if (cSet.in(strFmt('Cell%1%2%3', it, row, column)))
            #    {
            #        fsc = table6.controlNum((column - 1)*6 + row + 1);
            #    }
            #    else
            #    {
            #        fsc = table6.addControl(FormControlType::String, strFmt('Cell%1%2%3', it, row, column));
            #        fsc.alignment(3);
            #        fsc.colorScheme(FormColorScheme::RGB);
            #        fsc.limitText(50);
            #        fsc.multiLine(true);
            #        fsc.width(100);
            #        fsc.backgroundColor(WinAPI::rgb2int(hex2Int(element.getColor(it, 0, 0)),
            #                                            hex2Int(element.getColor(0, row, 0)),
            #                                            hex2Int(element.getColor(0, 0, column))));
            #
            #        if (row <= 3)
            #        {
            #            fsc.foregroundColor(WinAPI::rgb2int(0, 0, 0));
            #        }
            #        else
            #        {
            #            fsc.foregroundColor(WinAPI::rgb2int(255, 255, 255));
            #        }
            #
            #        // BP Deviation Documented
            #        table6.cell(column, row).data(strFmt('# %1\nR=%2\nG=%3\nB=%4',
            #                                    element.getColor(it, row, column),
            #                                    hex2Int(element.getColor(it, 0, 0)),
            #                                    hex2Int(element.getColor(0, row, 0)),
            #                                    hex2Int(element.getColor(0, 0, column))));
            #
            #        cSet.add(fsc.name());
            #    }
            #
            #    return fsc;
            #}
          ENDSOURCE
        ENDMETHODS
      ENDCONTROL 
      
      CONTROL TABPAGE
        PROPERTIES
          Name                #TabPage1
          ElementPosition     #536870910
          HierarchyParent     #Tab
          Caption             #Set 1
        ENDPROPERTIES
        
        METHODS
        ENDMETHODS
      ENDCONTROL 
      
      CONTROL TABPAGE
        PROPERTIES
          Name                #TabPage2
          ElementPosition     #805306365
          HierarchyParent     #Tab
          Caption             #Set 2
        ENDPROPERTIES
        
        METHODS
        ENDMETHODS
      ENDCONTROL 
      
      CONTROL TABPAGE
        PROPERTIES
          Name                #TabPage3
          ElementPosition     #1073741820
          HierarchyParent     #Tab
          Caption             #Set 3
        ENDPROPERTIES
        
        METHODS
        ENDMETHODS
      ENDCONTROL 
      
      CONTROL TABPAGE
        PROPERTIES
          Name                #TabPage4
          ElementPosition     #1342177275
          HierarchyParent     #Tab
          Caption             #Set 4
        ENDPROPERTIES
        
        METHODS
        ENDMETHODS
      ENDCONTROL 
      
      CONTROL TABPAGE
        PROPERTIES
          Name                #TabPage5
          ElementPosition     #1610612730
          HierarchyParent     #Tab
          Caption             #Set 5
        ENDPROPERTIES
        
        METHODS
        ENDMETHODS
      ENDCONTROL 
      
      CONTROL TABPAGE
        PROPERTIES
          Name                #TabPage6
          ElementPosition     #1879048185
          HierarchyParent     #Tab
          Caption             #Set 6
        ENDPROPERTIES
        
        METHODS
        ENDMETHODS
      ENDCONTROL 
      
      CONTROL STATICTEXT
        PROPERTIES
          Name                #MainTitleStaticText
          AutoDeclaration     #Yes
          ElementPosition     #1431655764
          HierarchyParent     #MainTitle
          Text                #Click on color to copy code into clipboard
          Style               #MainInstruction
        ENDPROPERTIES
        
        METHODS
        ENDMETHODS
      ENDCONTROL 
      
      CONTROL STRINGEDIT
        PROPERTIES
          Name                #Cell1
          ElementPosition     #1431655764
          HierarchyParent     #Table1
          LimitText           #50
          Alignment           #Center
          MultiLine           #Yes
        ENDPROPERTIES
        
        METHODS
          SOURCE #mouseDown
            #public int mouseDown(int _x, int _y, int _button, boolean _Ctrl, boolean _Shift)
            #{
            #    int ret;
            #
            #    ret = super(_x, _y, _button, _Ctrl, _Shift);
            #
            #    return ret;
            #}
          ENDSOURCE
        ENDMETHODS
      ENDCONTROL 
      
      CONTROL STRINGEDIT
        PROPERTIES
          Name                #Cell2
          ElementPosition     #1431655764
          HierarchyParent     #Table2
          LimitText           #50
          Alignment           #Center
          MultiLine           #Yes
        ENDPROPERTIES
        
        METHODS
        ENDMETHODS
      ENDCONTROL 
      
      CONTROL STRINGEDIT
        PROPERTIES
          Name                #Cell3
          ElementPosition     #1431655764
          HierarchyParent     #Table3
          LimitText           #50
          Alignment           #Center
          MultiLine           #Yes
        ENDPROPERTIES
        
        METHODS
        ENDMETHODS
      ENDCONTROL 
      
      CONTROL STRINGEDIT
        PROPERTIES
          Name                #Cell4
          ElementPosition     #1431655764
          HierarchyParent     #Table4
          LimitText           #50
          Alignment           #Center
          MultiLine           #Yes
        ENDPROPERTIES
        
        METHODS
        ENDMETHODS
      ENDCONTROL 
      
      CONTROL STRINGEDIT
        PROPERTIES
          Name                #Cell5
          ElementPosition     #1431655764
          HierarchyParent     #Table5
          LimitText           #50
          Alignment           #Center
          MultiLine           #Yes
        ENDPROPERTIES
        
        METHODS
        ENDMETHODS
      ENDCONTROL 
      
      CONTROL STRINGEDIT
        PROPERTIES
          Name                #Cell6
          ElementPosition     #1431655764
          HierarchyParent     #Table6
          LimitText           #50
          Alignment           #Center
          MultiLine           #Yes
        ENDPROPERTIES
        
        METHODS
        ENDMETHODS
      ENDCONTROL 
      
    ENDCONTAINER
    
  ENDDESIGN
  
  PERMISSIONS #Permissions
    PROPERTIES
    ENDPROPERTIES
    
    PERMISSIONSET #Read
      PROPERTIES
      ENDPROPERTIES
      
      FORM #Controls
      ENDFORM
      
    TABLES #Tables
    ENDTABLES
    
  SERVERMETHODS #Server Methods
  ENDSERVERMETHODS
  
ASSOCIATEDFORMS #Associated Forms
ENDASSOCIATEDFORMS

ENDPERMISSIONSET

PERMISSIONSET #Update
  PROPERTIES
  ENDPROPERTIES
  
  FORM #Controls
  ENDFORM
  
TABLES #Tables
ENDTABLES

SERVERMETHODS #Server Methods
ENDSERVERMETHODS

ASSOCIATEDFORMS #Associated Forms
ENDASSOCIATEDFORMS

ENDPERMISSIONSET

PERMISSIONSET #Create
  PROPERTIES
  ENDPROPERTIES
  
  FORM #Controls
  ENDFORM
  
TABLES #Tables
ENDTABLES

SERVERMETHODS #Server Methods
ENDSERVERMETHODS

ASSOCIATEDFORMS #Associated Forms
ENDASSOCIATEDFORMS

ENDPERMISSIONSET

PERMISSIONSET #Delete
  PROPERTIES
  ENDPROPERTIES
  
  FORM #Controls
  ENDFORM
  
TABLES #Tables
ENDTABLES

SERVERMETHODS #Server Methods
ENDSERVERMETHODS

ASSOCIATEDFORMS #Associated Forms
ENDASSOCIATEDFORMS

ENDPERMISSIONSET

ENDPERMISSIONS

ENDFORM


***Element: END
